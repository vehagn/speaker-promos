# Build output
/promo
/dist/

# Generated promos
/out/

# Local theme overrides. The promo manifest (promos.yaml) is deliberately NOT
# ignored: it is the record of every correction made to the guessed data, and
# reviewing it in a diff is the point.
/theme.yaml

# Editors
.idea/
.DS_Store
